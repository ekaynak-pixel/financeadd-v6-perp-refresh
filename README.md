# FinanceAdd (v4 with devDependencies)
Includes TypeScript devDependencies so Vercel build won't fail on typings. Sitemap/robots/RSS, CSE-first, AI+web results, auto blog stub, IndexNow key route, Node runtime.
