export async function pingIndexNow(urls:string[]){const key=process.env.INDEXNOW_KEY||'';if(!key||urls.length===0) return {ok:false,message:'Missing key or urls'};try{const base=process.env.SITE_URL||'https://financeadd.com';const body={host:new URL(base).host,key:key,keyLocation:`${base}/${key}.txt`,urlList:urls};const res=await fetch('https://api.indexnow.org/indexnow',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});return {ok:res.ok,status:res.status}}catch(e:any){return {ok:false,message:e.message}}}
